module.exports.configs = {
  'default-config': {
    component: {
      name: 'tunnel-proxy',
      config: {
        startMethod: 'start',
        schema: {
          methods: {
            'start': {
              parameters: [{
                name: 'opts',
                value: {
                  // tunnelComponentName: 'tunnel-service',
                  // tunnelEndpointName: undefined, // if tunnel component is remote
                  listenPort: process.env.PROXY_PORT ? parseInt(process.env.PROXY_PORT) : 80
                }
              }]
            }
          }
        }
      }
    }
  }
}